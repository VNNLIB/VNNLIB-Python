## Installing from source

```bash
git clone --recurse-submodules https://github.com/VNNLIB/VNNLIB-Python.git
cd VNNLIB-Python
pip install -e .
```

## Updating the C++ bindings

If the underlying [C++ bindings](https://github.com/VNNLIB/VNNLIB-CPP) change then:

1. TODO

## Making a new release

1. Update `README.md` with new entry to compatability table. 

2. TODO